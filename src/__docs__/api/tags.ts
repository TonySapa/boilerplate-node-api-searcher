import { routerDescription as entriesDescription } from '../api/entries'
import { routerDescription as firmsDescription } from '../api/firms'
import { routerDescription as userDescription } from '../api/users'

export const tags = [
  entriesDescription,
  firmsDescription,
  userDescription
]
